From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: =?UTF-8?q?Carlo=20Maria=20Card=C3=AD?=
 <112445885+MathsAnalysis@users.noreply.github.com>
Date: Thu, 4 Jun 2026 02:12:32 +0200
Subject: [PATCH] Petal Async Pathfinding


diff --git a/net/minecraft/world/entity/ai/behavior/AcquirePoi.java b/net/minecraft/world/entity/ai/behavior/AcquirePoi.java
index 812cd00f750197f9bdde6f2295c27172ae463414..e9eaef24da92223cfa8fb3e19fabe87924e4536f 100644
--- a/net/minecraft/world/entity/ai/behavior/AcquirePoi.java
+++ b/net/minecraft/world/entity/ai/behavior/AcquirePoi.java
@@ -58,11 +58,23 @@ public class AcquirePoi {
         int rate = 20;
         MutableLong nextScheduledStart = new MutableLong(0L);
         Long2ObjectMap<AcquirePoi.JitteredLinearRetry> batchCache = new Long2ObjectOpenHashMap<>();
+        // Purpur start - Async Pathfinding - per-instance pending state for polling (DivineMC)
+        final PendingPath[] pending = new PendingPath[1];
+        // Purpur end - Async Pathfinding (DivineMC)
         OneShot<PathfinderMob> acquirePoi = BehaviorBuilder.create(
             i -> i.group(i.absent(memoryToAcquire))
                 .apply(
                     i,
                     toAcquire -> (level, body, timestamp) -> {
+                        // Purpur start - Async Pathfinding - drain pending async path on main thread before doing anything else (DivineMC)
+                        PendingPath pendingPath = pending[0];
+                        if (pendingPath != null) {
+                            if (!pendingPath.path.isProcessed()) return false;
+                            pending[0] = null;
+                            applyPathResult(poiType, onPoiAcquisitionEvent, batchCache, toAcquire, level, body, timestamp, level.getPoiManager(), pendingPath.pois, pendingPath.path);
+                            return true;
+                        }
+                        // Purpur end - Async Pathfinding (DivineMC)
                         if (onlyIfAdult && body.isBaby()) {
                             return false;
                         }
@@ -104,21 +116,14 @@ public class AcquirePoi {
                             }
                         }
                         // Paper end - optimise POI searches
+                        // Purpur start - Async Pathfinding - defer instead of using callback so memory is set on main thread (DivineMC)
                         Path path = findPathToPois(body, poiPositions);
-                        if (path != null && path.canReach()) {
-                            BlockPos targetPos = path.getTarget();
-                            poiManager.getType(targetPos).ifPresent(type -> {
-                                poiManager.take(poiType, (t, poiPos) -> poiPos.equals(targetPos), targetPos, 1);
-                                toAcquire.set(GlobalPos.of(level.dimension(), targetPos));
-                                onPoiAcquisitionEvent.ifPresent(event -> level.broadcastEntityEvent(body, event));
-                                batchCache.clear();
-                                level.debugSynchronizers().updatePoi(targetPos);
-                            });
-                        } else {
-                            for (Pair<Holder<PoiType>, BlockPos> p : poiPositions) {
-                                batchCache.computeIfAbsent(p.getSecond().asLong(), key -> new AcquirePoi.JitteredLinearRetry(random, timestamp));
-                            }
+                        if (path instanceof org.bxteam.divinemc.async.pathfinding.AsyncPath asyncPath && !asyncPath.isProcessed()) {
+                            pending[0] = new PendingPath(path, poiPositions);
+                            return false;
                         }
+                        applyPathResult(poiType, onPoiAcquisitionEvent, batchCache, toAcquire, level, body, timestamp, poiManager, poiPositions, path);
+                        // Purpur end - Async Pathfinding (DivineMC)
 
                         return true;
                     }
@@ -129,6 +134,36 @@ public class AcquirePoi {
             : BehaviorBuilder.create(i -> i.group(i.absent(memoryToValidate)).apply(i, toValidate -> acquirePoi));
     }
 
+    // Purpur start - Async Pathfinding (DivineMC)
+    private record PendingPath(Path path, Set<Pair<Holder<PoiType>, BlockPos>> pois) {}
+
+    private static void applyPathResult(final Predicate<Holder<PoiType>> poiType,
+                                        final Optional<Byte> onPoiAcquisitionEvent,
+                                        final Long2ObjectMap<JitteredLinearRetry> batchCache,
+                                        final net.minecraft.world.entity.ai.behavior.declarative.MemoryAccessor<com.mojang.datafixers.kinds.Const.Mu<com.mojang.datafixers.util.Unit>, GlobalPos> toAcquire,
+                                        final ServerLevel level,
+                                        final PathfinderMob body,
+                                        final long timestamp,
+                                        final PoiManager poiManager,
+                                        final Set<Pair<Holder<PoiType>, BlockPos>> poiPositions,
+                                        final @Nullable Path path) {
+        if (path != null && path.canReach()) {
+            BlockPos targetPos = path.getTarget();
+            poiManager.getType(targetPos).ifPresent(type -> {
+                poiManager.take(poiType, (t, poiPos) -> poiPos.equals(targetPos), targetPos, 1);
+                toAcquire.set(GlobalPos.of(level.dimension(), targetPos));
+                onPoiAcquisitionEvent.ifPresent(event -> level.broadcastEntityEvent(body, event));
+                batchCache.clear();
+                level.debugSynchronizers().updatePoi(targetPos);
+            });
+        } else {
+            for (Pair<Holder<PoiType>, BlockPos> p : poiPositions) {
+                batchCache.computeIfAbsent(p.getSecond().asLong(), key -> new JitteredLinearRetry(level.getRandom(), timestamp));
+            }
+        }
+    }
+    // Purpur end - Async Pathfinding (DivineMC)
+
     public static @Nullable Path findPathToPois(final Mob body, final Set<Pair<Holder<PoiType>, BlockPos>> pois) {
         if (pois.isEmpty()) {
             return null;
diff --git a/net/minecraft/world/entity/ai/behavior/MoveToTargetSink.java b/net/minecraft/world/entity/ai/behavior/MoveToTargetSink.java
index b40da004b5281a041ee896ae176bfb9c4660f353..e1d9c77bf6fc823a22ca0d4d795ed316eea98018 100644
--- a/net/minecraft/world/entity/ai/behavior/MoveToTargetSink.java
+++ b/net/minecraft/world/entity/ai/behavior/MoveToTargetSink.java
@@ -20,6 +20,7 @@ public class MoveToTargetSink extends Behavior<Mob> {
     private static final int MAX_COOLDOWN_BEFORE_RETRYING = 40;
     private int remainingCooldown;
     private @Nullable Path path;
+    private boolean finishedProcessing; // Purpur - Async Pathfinding (DivineMC)
     private @Nullable BlockPos lastTargetPos;
     private float speedModifier;
 
@@ -52,9 +53,11 @@ public class MoveToTargetSink extends Behavior<Mob> {
         Brain<?> brain = body.getBrain();
         WalkTarget walkTarget = brain.getMemory(MemoryModuleType.WALK_TARGET).get();
         boolean reachedTarget = this.reachedTarget(body, walkTarget);
-        if (!reachedTarget && this.tryComputePath(body, walkTarget, level.getGameTime())) {
+        if (!org.bxteam.divinemc.config.DivineConfig.AsyncCategory.asyncPathfinding && !reachedTarget && this.tryComputePath(body, walkTarget, level.getGameTime())) { // Purpur - Async Pathfinding (DivineMC)
             this.lastTargetPos = walkTarget.getTarget().currentBlockPosition();
             return true;
+        } else if (org.bxteam.divinemc.config.DivineConfig.AsyncCategory.asyncPathfinding && !reachedTarget) { // Purpur - Async Pathfinding (DivineMC)
+            return true;
         }
 
         brain.eraseMemory(MemoryModuleType.WALK_TARGET);
@@ -67,6 +70,7 @@ public class MoveToTargetSink extends Behavior<Mob> {
 
     @Override
     protected boolean canStillUse(final ServerLevel level, final Mob body, final long timestamp) {
+        if (org.bxteam.divinemc.config.DivineConfig.AsyncCategory.asyncPathfinding && !this.finishedProcessing) return true; // Purpur - wait for processing (DivineMC)
         if (this.path != null && this.lastTargetPos != null) {
             Optional<WalkTarget> walkTarget = body.getBrain().getMemory(MemoryModuleType.WALK_TARGET);
             boolean isSpectator = walkTarget.map(MoveToTargetSink::isWalkTargetSpectator).orElse(false);
@@ -93,27 +97,97 @@ public class MoveToTargetSink extends Behavior<Mob> {
 
     @Override
     protected void start(final ServerLevel level, final Mob body, final long timestamp) {
+        // Purpur start - Async Pathfinding - start processing (DivineMC)
+        if (org.bxteam.divinemc.config.DivineConfig.AsyncCategory.asyncPathfinding) {
+            Brain<?> brain = body.getBrain();
+            WalkTarget walkTarget = brain.getMemory(MemoryModuleType.WALK_TARGET).get();
+
+            this.finishedProcessing = false;
+            this.lastTargetPos = walkTarget.getTarget().currentBlockPosition();
+            this.path = this.computePath(body, walkTarget);
+            return;
+        }
+        // Purpur end - Async Pathfinding (DivineMC)
         body.getBrain().setMemory(MemoryModuleType.PATH, this.path);
         body.getNavigation().moveTo(this.path, this.speedModifier);
     }
 
     @Override
     protected void tick(final ServerLevel level, final Mob body, final long timestamp) {
-        Path newPath = body.getNavigation().getPath();
-        Brain<?> brain = body.getBrain();
-        if (this.path != newPath) {
-            this.path = newPath;
-            brain.setMemory(MemoryModuleType.PATH, newPath);
-        }
+        // Purpur start - Async Pathfinding (DivineMC)
+        if (org.bxteam.divinemc.config.DivineConfig.AsyncCategory.asyncPathfinding) {
+            if (this.path != null && !this.path.isProcessed()) return;
+
+            if (!this.finishedProcessing) {
+                this.finishedProcessing = true;
+
+                Brain<?> brain = body.getBrain();
+                boolean canReach = this.path != null && this.path.canReach();
+                if (canReach) {
+                    brain.eraseMemory(MemoryModuleType.CANT_REACH_WALK_TARGET_SINCE);
+                } else if (!brain.hasMemoryValue(MemoryModuleType.CANT_REACH_WALK_TARGET_SINCE)) {
+                    brain.setMemory(MemoryModuleType.CANT_REACH_WALK_TARGET_SINCE, timestamp);
+                }
+
+                if (!canReach) {
+                    Optional<WalkTarget> walkTarget = brain.getMemory(MemoryModuleType.WALK_TARGET);
+
+                    if (!walkTarget.isPresent()) return;
+
+                    BlockPos targetPos = walkTarget.get().getTarget().currentBlockPosition();
+                    Vec3 partialStep = DefaultRandomPos.getPosTowards((PathfinderMob) body, 10, 7, Vec3.atBottomCenterOf(targetPos), (float) Math.PI / 2F);
+                    if (partialStep != null) {
+                        // try recalculating the path using a random position
+                        this.path = body.getNavigation().createPath(partialStep.x, partialStep.y, partialStep.z, 0);
+                        this.finishedProcessing = false;
+                        return;
+                    }
+                }
+
+                body.getBrain().setMemory(MemoryModuleType.PATH, this.path);
+                body.getNavigation().moveTo(this.path, this.speedModifier);
+            }
 
-        if (newPath != null && this.lastTargetPos != null) {
-            WalkTarget walkTarget = brain.getMemory(MemoryModuleType.WALK_TARGET).get();
-            if (walkTarget.getTarget().currentBlockPosition().distSqr(this.lastTargetPos) > 4.0 && this.tryComputePath(body, walkTarget, level.getGameTime())) {
-                this.lastTargetPos = walkTarget.getTarget().currentBlockPosition();
-                this.start(level, body, timestamp);
+            Path newPath = body.getNavigation().getPath();
+            Brain<?> brain = body.getBrain();
+
+            if (newPath != null && this.lastTargetPos != null && brain.hasMemoryValue(MemoryModuleType.WALK_TARGET)) {
+                WalkTarget walkTarget = brain.getMemory(MemoryModuleType.WALK_TARGET).get(); // we know isPresent = true
+                if (walkTarget.getTarget().currentBlockPosition().distSqr(this.lastTargetPos) > 4.0D) {
+                    this.start(level, body, timestamp);
+                }
+            }
+        } else {
+            Path newPath = body.getNavigation().getPath();
+            Brain<?> brain = body.getBrain();
+            if (this.path != newPath) {
+                this.path = newPath;
+                brain.setMemory(MemoryModuleType.PATH, newPath);
+            }
+
+            if (newPath != null && this.lastTargetPos != null) {
+                WalkTarget walkTarget = brain.getMemory(MemoryModuleType.WALK_TARGET).get();
+                if (walkTarget.getTarget().currentBlockPosition().distSqr(this.lastTargetPos) > 4.0 && this.tryComputePath(body, walkTarget, level.getGameTime())) {
+                    this.lastTargetPos = walkTarget.getTarget().currentBlockPosition();
+                    this.start(level, body, timestamp);
+                }
             }
         }
+        // Purpur end - Async Pathfinding (DivineMC)
+    }
+
+    // Purpur start - Async Pathfinding (DivineMC)
+    @Nullable
+    private Path computePath(final Mob body, final WalkTarget walkTarget) {
+        BlockPos targetPos = walkTarget.getTarget().currentBlockPosition();
+        this.speedModifier = walkTarget.getSpeedModifier();
+        Brain<?> brain = body.getBrain();
+        if (this.reachedTarget(body, walkTarget)) {
+            brain.eraseMemory(MemoryModuleType.CANT_REACH_WALK_TARGET_SINCE);
+        }
+        return body.getNavigation().createPath(targetPos, 0);
     }
+    // Purpur end - Async Pathfinding (DivineMC)
 
     private boolean tryComputePath(final Mob body, final WalkTarget walkTarget, final long timestamp) {
         BlockPos targetPos = walkTarget.getTarget().currentBlockPosition();
diff --git a/net/minecraft/world/entity/ai/behavior/SetClosestHomeAsWalkTarget.java b/net/minecraft/world/entity/ai/behavior/SetClosestHomeAsWalkTarget.java
index 5bd7473b0db1a87f905c01c19ddf99e3a447bcfa..8476b3f0ed0b248161fb43dc74e2beedf02330e7 100644
--- a/net/minecraft/world/entity/ai/behavior/SetClosestHomeAsWalkTarget.java
+++ b/net/minecraft/world/entity/ai/behavior/SetClosestHomeAsWalkTarget.java
@@ -29,11 +29,23 @@ public class SetClosestHomeAsWalkTarget {
     public static BehaviorControl<PathfinderMob> create(final float speedModifier) {
         Long2LongMap batchCache = new Long2LongOpenHashMap();
         MutableLong lastUpdate = new MutableLong(0L);
+        // Purpur start - Async Pathfinding - per-instance pending state for polling (DivineMC)
+        final Pending[] pending = new Pending[1];
+        // Purpur end - Async Pathfinding (DivineMC)
         return BehaviorBuilder.create(
             i -> i.group(i.absent(MemoryModuleType.WALK_TARGET), i.absent(MemoryModuleType.HOME))
                 .apply(
                     i,
                     (walkTarget, home) -> (level, body, timestamp) -> {
+                        // Purpur start - Async Pathfinding - drain pending async path on main thread before doing anything else (DivineMC)
+                        Pending pendingPath = pending[0];
+                        if (pendingPath != null) {
+                            if (!pendingPath.path.isProcessed()) return false;
+                            pending[0] = null;
+                            applyPathResult(speedModifier, batchCache, lastUpdate, walkTarget, level, level.getPoiManager(), pendingPath.tried, pendingPath.path);
+                            return true;
+                        }
+                        // Purpur end - Async Pathfinding (DivineMC)
                         if (level.getGameTime() - lastUpdate.longValue() < RATE) {
                             return false;
                         }
@@ -62,17 +74,14 @@ public class SetClosestHomeAsWalkTarget {
                                     p -> p.is(PoiTypes.HOME), cacheTest, body.blockPosition(), AcquirePoi.SCAN_RANGE, PoiManager.Occupancy.ANY
                                 )
                                 .collect(Collectors.toSet());
+                            // Purpur start - Async Pathfinding - defer instead of using callback so memory is set on main thread (DivineMC)
                             Path path = AcquirePoi.findPathToPois(body, pois);
-                            if (path != null && path.canReach()) {
-                                BlockPos targetPos = path.getTarget();
-                                Optional<Holder<PoiType>> type = poiManager.getType(targetPos);
-                                if (type.isPresent()) {
-                                    walkTarget.set(new WalkTarget(targetPos, speedModifier, 1));
-                                    level.debugSynchronizers().updatePoi(targetPos);
-                                }
-                            } else if (triedCount.intValue() < BATCH_SIZE) {
-                                batchCache.long2LongEntrySet().removeIf(entry -> entry.getLongValue() < lastUpdate.longValue());
+                            if (path instanceof org.bxteam.divinemc.async.pathfinding.AsyncPath asyncPath && !asyncPath.isProcessed()) {
+                                pending[0] = new Pending(path, triedCount.intValue());
+                                return false;
                             }
+                            applyPathResult(speedModifier, batchCache, lastUpdate, walkTarget, level, poiManager, triedCount.intValue(), path);
+                            // Purpur end - Async Pathfinding (DivineMC)
 
                             return true;
                         } else {
@@ -82,4 +91,28 @@ public class SetClosestHomeAsWalkTarget {
                 )
         );
     }
+
+    // Purpur start - Async Pathfinding (DivineMC)
+    private record Pending(Path path, int tried) {}
+
+    private static void applyPathResult(final float speedModifier,
+                                        final Long2LongMap batchCache,
+                                        final MutableLong lastUpdate,
+                                        final net.minecraft.world.entity.ai.behavior.declarative.MemoryAccessor<com.mojang.datafixers.kinds.Const.Mu<com.mojang.datafixers.util.Unit>, WalkTarget> walkTarget,
+                                        final net.minecraft.server.level.ServerLevel level,
+                                        final PoiManager poiManager,
+                                        final int tried,
+                                        final @org.jetbrains.annotations.Nullable Path path) {
+        if (path != null && path.canReach()) {
+            BlockPos targetPos = path.getTarget();
+            Optional<Holder<PoiType>> type = poiManager.getType(targetPos);
+            if (type.isPresent()) {
+                walkTarget.set(new WalkTarget(targetPos, speedModifier, 1));
+                level.debugSynchronizers().updatePoi(targetPos);
+            }
+        } else if (tried < BATCH_SIZE) {
+            batchCache.long2LongEntrySet().removeIf(entry -> entry.getLongValue() < lastUpdate.longValue());
+        }
+    }
+    // Purpur end - Async Pathfinding (DivineMC)
 }
diff --git a/net/minecraft/world/entity/ai/goal/DoorInteractGoal.java b/net/minecraft/world/entity/ai/goal/DoorInteractGoal.java
index 0b383b16145d44e5dc29b8de123546656b4793dd..6db0207147b7321bc79f2ee417f7fd39065e9550 100644
--- a/net/minecraft/world/entity/ai/goal/DoorInteractGoal.java
+++ b/net/minecraft/world/entity/ai/goal/DoorInteractGoal.java
@@ -57,7 +57,7 @@ public abstract class DoorInteractGoal extends Goal {
         }
 
         Path path = this.mob.getNavigation().getPath();
-        if (path != null && !path.isDone()) {
+        if (path != null && path.isProcessed() && !path.isDone()) { // Purpur - Async Pathfinding (DivineMC)
             for (int i = 0; i < Math.min(path.getNextNodeIndex() + 2, path.getNodeCount()); i++) {
                 Node node = path.getNode(i);
                 this.doorPos = new BlockPos(node.x, node.y + 1, node.z);
diff --git a/net/minecraft/world/entity/ai/navigation/AmphibiousPathNavigation.java b/net/minecraft/world/entity/ai/navigation/AmphibiousPathNavigation.java
index 45942481a972f2308eae80f6f0f8b886e47016ec..cade1041cce909436c944676fb7a7a3235ab7bd8 100644
--- a/net/minecraft/world/entity/ai/navigation/AmphibiousPathNavigation.java
+++ b/net/minecraft/world/entity/ai/navigation/AmphibiousPathNavigation.java
@@ -12,9 +12,25 @@ public class AmphibiousPathNavigation extends PathNavigation {
         super(mob, level);
     }
 
+    // Purpur start - Async Pathfinding (DivineMC)
+    private static final org.bxteam.divinemc.async.pathfinding.NodeEvaluatorGenerator nodeEvaluatorGenerator = (org.bxteam.divinemc.async.pathfinding.NodeEvaluatorFeatures nodeEvaluatorFeatures) -> {
+        AmphibiousNodeEvaluator nodeEvaluator = new AmphibiousNodeEvaluator(false);
+        nodeEvaluator.setCanPassDoors(nodeEvaluatorFeatures.canPassDoors());
+        nodeEvaluator.setCanFloat(nodeEvaluatorFeatures.canFloat());
+        nodeEvaluator.setCanWalkOverFences(nodeEvaluatorFeatures.canWalkOverFences());
+        nodeEvaluator.setCanOpenDoors(nodeEvaluatorFeatures.canOpenDoors());
+        return nodeEvaluator;
+    };
+    // Purpur end - Async Pathfinding (DivineMC)
+
     @Override
     protected PathFinder createPathFinder(final int maxVisitedNodes) {
         this.nodeEvaluator = new AmphibiousNodeEvaluator(false);
+        // Purpur start - Async Pathfinding (DivineMC)
+        if (org.bxteam.divinemc.config.DivineConfig.AsyncCategory.asyncPathfinding) {
+            return new PathFinder(this.nodeEvaluator, maxVisitedNodes, nodeEvaluatorGenerator);
+        }
+        // Purpur end - Async Pathfinding (DivineMC)
         return new PathFinder(this.nodeEvaluator, maxVisitedNodes);
     }
 
diff --git a/net/minecraft/world/entity/ai/navigation/FlyingPathNavigation.java b/net/minecraft/world/entity/ai/navigation/FlyingPathNavigation.java
index e44814cfb6afb594456b8215bd13a92e93de2c85..3a9e8a596d607b4d34110b5921f64b489fd8066a 100644
--- a/net/minecraft/world/entity/ai/navigation/FlyingPathNavigation.java
+++ b/net/minecraft/world/entity/ai/navigation/FlyingPathNavigation.java
@@ -15,9 +15,25 @@ public class FlyingPathNavigation extends PathNavigation {
         super(mob, level);
     }
 
+    // Purpur start - Async Pathfinding (DivineMC)
+    private static final org.bxteam.divinemc.async.pathfinding.NodeEvaluatorGenerator nodeEvaluatorGenerator = (org.bxteam.divinemc.async.pathfinding.NodeEvaluatorFeatures nodeEvaluatorFeatures) -> {
+        FlyNodeEvaluator nodeEvaluator = new FlyNodeEvaluator();
+        nodeEvaluator.setCanPassDoors(nodeEvaluatorFeatures.canPassDoors());
+        nodeEvaluator.setCanFloat(nodeEvaluatorFeatures.canFloat());
+        nodeEvaluator.setCanWalkOverFences(nodeEvaluatorFeatures.canWalkOverFences());
+        nodeEvaluator.setCanOpenDoors(nodeEvaluatorFeatures.canOpenDoors());
+        return nodeEvaluator;
+    };
+    // Purpur end - Async Pathfinding (DivineMC)
+
     @Override
     protected PathFinder createPathFinder(final int maxVisitedNodes) {
         this.nodeEvaluator = new FlyNodeEvaluator();
+        // Purpur start - Async Pathfinding (DivineMC)
+        if (org.bxteam.divinemc.config.DivineConfig.AsyncCategory.asyncPathfinding) {
+            return new PathFinder(this.nodeEvaluator, maxVisitedNodes, nodeEvaluatorGenerator);
+        }
+        // Purpur end - Async Pathfinding (DivineMC)
         return new PathFinder(this.nodeEvaluator, maxVisitedNodes);
     }
 
@@ -47,6 +63,7 @@ public class FlyingPathNavigation extends PathNavigation {
         if (this.hasDelayedRecomputation) {
             this.recomputePath();
         }
+        if (this.path != null && !this.path.isProcessed()) return; // Purpur - Async Pathfinding (DivineMC)
 
         if (!this.isDone()) {
             if (this.canUpdatePath()) {
diff --git a/net/minecraft/world/entity/ai/navigation/GroundPathNavigation.java b/net/minecraft/world/entity/ai/navigation/GroundPathNavigation.java
index 2d0531aed8a3274d8673b0950eb284101d933755..bae17904eb145b568e0e5ada08e78820ba5b79f5 100644
--- a/net/minecraft/world/entity/ai/navigation/GroundPathNavigation.java
+++ b/net/minecraft/world/entity/ai/navigation/GroundPathNavigation.java
@@ -25,9 +25,25 @@ public class GroundPathNavigation extends PathNavigation {
         super(mob, level);
     }
 
+    // Purpur start - Async Pathfinding (DivineMC)
+    protected static final org.bxteam.divinemc.async.pathfinding.NodeEvaluatorGenerator nodeEvaluatorGenerator = (org.bxteam.divinemc.async.pathfinding.NodeEvaluatorFeatures nodeEvaluatorFeatures) -> {
+        WalkNodeEvaluator nodeEvaluator = new WalkNodeEvaluator();
+        nodeEvaluator.setCanPassDoors(nodeEvaluatorFeatures.canPassDoors());
+        nodeEvaluator.setCanFloat(nodeEvaluatorFeatures.canFloat());
+        nodeEvaluator.setCanWalkOverFences(nodeEvaluatorFeatures.canWalkOverFences());
+        nodeEvaluator.setCanOpenDoors(nodeEvaluatorFeatures.canOpenDoors());
+        return nodeEvaluator;
+    };
+    // Purpur end - Async Pathfinding (DivineMC)
+
     @Override
     protected PathFinder createPathFinder(final int maxVisitedNodes) {
         this.nodeEvaluator = new WalkNodeEvaluator();
+        // Purpur start - Async Pathfinding (DivineMC)
+        if (org.bxteam.divinemc.config.DivineConfig.AsyncCategory.asyncPathfinding) {
+            return new PathFinder(this.nodeEvaluator, maxVisitedNodes, nodeEvaluatorGenerator);
+        }
+        // Purpur end - Async Pathfinding (DivineMC)
         return new PathFinder(this.nodeEvaluator, maxVisitedNodes);
     }
 
diff --git a/net/minecraft/world/entity/ai/navigation/PathNavigation.java b/net/minecraft/world/entity/ai/navigation/PathNavigation.java
index 1eb0afab932c3828a1f19861994b63b247df0fbb..bcfb3ea3a4d1337ebb131e345cf8c931c1ac07bc 100644
--- a/net/minecraft/world/entity/ai/navigation/PathNavigation.java
+++ b/net/minecraft/world/entity/ai/navigation/PathNavigation.java
@@ -167,6 +167,12 @@ public abstract class PathNavigation {
             return null;
         }
 
+        // Purpur start - Async Pathfinding (DivineMC)
+        if (this.path instanceof org.bxteam.divinemc.async.pathfinding.AsyncPath asyncPath && !asyncPath.isProcessed() && asyncPath.hasSameTargetPositions(targets)) {
+            return this.path;
+        }
+        // Purpur end - Async Pathfinding (DivineMC)
+
         if (this.path != null && !this.path.isDone() && targets.contains(this.targetPos)) {
             return this.path;
         }
@@ -194,12 +200,30 @@ public abstract class PathNavigation {
         int radius = (int)(maxPathLength + radiusOffset);
         PathNavigationRegion region = new PathNavigationRegion(this.level, fromPos.offset(-radius, -radius, -radius), fromPos.offset(radius, radius, radius));
         Path path = this.pathFinder.findPath(region, this.mob, targets, maxPathLength, reachRange, this.maxVisitedNodesMultiplier);
-        profiler.pop();
-        if (path != null && path.getTarget() != null) {
-            this.targetPos = path.getTarget();
-            this.reachRange = reachRange;
-            this.resetStuckTimeout();
+        // Purpur start - Async Pathfinding (DivineMC)
+        if (org.bxteam.divinemc.config.DivineConfig.AsyncCategory.asyncPathfinding) {
+            // assign early a target position. most calls will only have 1 position
+            if (!targets.isEmpty()) this.targetPos = targets.iterator().next();
+
+            org.bxteam.divinemc.async.pathfinding.AsyncPath.applyAfterProcessing(path, processedPath -> {
+                if (processedPath != this.path) return;
+
+                if (processedPath != null && processedPath.getTarget() != null) {
+                    profiler.pop();
+                    this.targetPos = processedPath.getTarget();
+                    this.reachRange = reachRange;
+                    this.resetStuckTimeout();
+                }
+            });
+        } else {
+            profiler.pop();
+            if (path != null && path.getTarget() != null) {
+                this.targetPos = path.getTarget();
+                this.reachRange = reachRange;
+                this.resetStuckTimeout();
+            }
         }
+        // Purpur end - Async Pathfinding (DivineMC)
 
         return path;
     }
@@ -251,8 +275,8 @@ public abstract class PathNavigation {
             return false;
         }
 
-        this.trimPath();
-        if (this.path.getNodeCount() <= 0) {
+        if (path.isProcessed()) this.trimPath(); // Purpur - only trim if processed (DivineMC)
+        if (path.isProcessed() && this.path.getNodeCount() <= 0) { // Purpur - only check node count if processed (DivineMC)
             return false;
         }
 
@@ -272,6 +296,7 @@ public abstract class PathNavigation {
         if (this.hasDelayedRecomputation) {
             this.recomputePath();
         }
+        if (this.path != null && !this.path.isProcessed()) return; // Purpur - skip pathfinding if we're still processing (DivineMC)
 
         if (!this.isDone()) {
             if (this.canUpdatePath()) {
@@ -297,6 +322,7 @@ public abstract class PathNavigation {
     }
 
     protected void followThePath() {
+        if (!this.path.isProcessed()) return; // Purpur - skip if not processed (DivineMC)
         Vec3 mobPos = this.getTempMobPos();
         this.maxDistanceToWaypoint = this.mob.getBbWidth() > 0.75F ? this.mob.getBbWidth() / 2.0F : 0.75F - this.mob.getBbWidth() / 2.0F;
         Vec3i currentNodePos = this.path.getNextNodePos();
@@ -455,7 +481,7 @@ public abstract class PathNavigation {
     public boolean shouldRecomputePath(final BlockPos pos) {
         if (this.hasDelayedRecomputation) {
             return false;
-        } else if (this.path != null && !this.path.isDone() && this.path.getNodeCount() != 0) {
+        } else if (this.path != null && this.path.isProcessed() && !this.path.isDone() && this.path.getNodeCount() != 0) { // Purpur - Skip if not processed (DivineMC)
             Node target = this.path.getEndNode();
             Vec3 middlePos = new Vec3((target.x + this.mob.getX()) / 2.0, (target.y + this.mob.getY()) / 2.0, (target.z + this.mob.getZ()) / 2.0);
             return pos.closerToCenterThan(middlePos, this.path.getNodeCount() - this.path.getNextNodeIndex());
diff --git a/net/minecraft/world/entity/ai/navigation/WaterBoundPathNavigation.java b/net/minecraft/world/entity/ai/navigation/WaterBoundPathNavigation.java
index bf7c7dfc9dabe905490a9610b4201a0023ef69b3..4e85792419014467742a2111cf7cad83fc961d10 100644
--- a/net/minecraft/world/entity/ai/navigation/WaterBoundPathNavigation.java
+++ b/net/minecraft/world/entity/ai/navigation/WaterBoundPathNavigation.java
@@ -15,11 +15,27 @@ public class WaterBoundPathNavigation extends PathNavigation {
         super(mob, level);
     }
 
+    // Purpur start - Async Pathfinding (DivineMC)
+    private static final org.bxteam.divinemc.async.pathfinding.NodeEvaluatorGenerator nodeEvaluatorGenerator = (org.bxteam.divinemc.async.pathfinding.NodeEvaluatorFeatures nodeEvaluatorFeatures) -> {
+        SwimNodeEvaluator nodeEvaluator = new SwimNodeEvaluator(nodeEvaluatorFeatures.allowBreaching());
+        nodeEvaluator.setCanPassDoors(nodeEvaluatorFeatures.canPassDoors());
+        nodeEvaluator.setCanFloat(nodeEvaluatorFeatures.canFloat());
+        nodeEvaluator.setCanWalkOverFences(nodeEvaluatorFeatures.canWalkOverFences());
+        nodeEvaluator.setCanOpenDoors(nodeEvaluatorFeatures.canOpenDoors());
+        return nodeEvaluator;
+    };
+    // Purpur end - Async Pathfinding (DivineMC)
+
     @Override
     protected PathFinder createPathFinder(final int maxVisitedNodes) {
         this.allowBreaching = this.mob.is(EntityType.DOLPHIN);
         this.nodeEvaluator = new SwimNodeEvaluator(this.allowBreaching);
         this.nodeEvaluator.setCanPassDoors(false);
+        // Purpur start - Async Pathfinding (DivineMC)
+        if (org.bxteam.divinemc.config.DivineConfig.AsyncCategory.asyncPathfinding) {
+            return new PathFinder(this.nodeEvaluator, maxVisitedNodes, nodeEvaluatorGenerator);
+        }
+        // Purpur end - Async Pathfinding (DivineMC)
         return new PathFinder(this.nodeEvaluator, maxVisitedNodes);
     }
 
diff --git a/net/minecraft/world/entity/ai/sensing/NearestBedSensor.java b/net/minecraft/world/entity/ai/sensing/NearestBedSensor.java
index 535cdcd8266dced3aca0e6b71806bbbd80c70228..fbd427b7b67007f066c41065a2915e7b6faa313a 100644
--- a/net/minecraft/world/entity/ai/sensing/NearestBedSensor.java
+++ b/net/minecraft/world/entity/ai/sensing/NearestBedSensor.java
@@ -26,6 +26,7 @@ public class NearestBedSensor extends Sensor<Mob> {
     private final Long2LongMap batchCache = new Long2LongOpenHashMap();
     private int triedCount;
     private long lastUpdate;
+    private @org.jetbrains.annotations.Nullable Path pending; // Purpur - Async Pathfinding (DivineMC)
 
     public NearestBedSensor() {
         super(20);
@@ -38,6 +39,15 @@ public class NearestBedSensor extends Sensor<Mob> {
 
     @Override
     protected void doTick(final ServerLevel level, final Mob body) {
+        // Purpur start - Async Pathfinding - drain pending async path on main thread (DivineMC)
+        if (this.pending != null) {
+            if (!this.pending.isProcessed()) return;
+            Path done = this.pending;
+            this.pending = null;
+            applyPathResult(body, level.getPoiManager(), done);
+            return;
+        }
+        // Purpur end - Async Pathfinding (DivineMC)
         if (body.isBaby()) {
             this.triedCount = 0;
             this.lastUpdate = level.getGameTime() + level.getRandom().nextInt(20);
@@ -59,16 +69,28 @@ public class NearestBedSensor extends Sensor<Mob> {
                     e -> e.is(PoiTypes.HOME), cacheTest, body.blockPosition(), level.purpurConfig.villagerNearestBedSensorSearchRadius, PoiManager.Occupancy.ANY // Purpur - Configurable villager search radius
                 )
                 .collect(Collectors.toSet());
+            // Purpur start - Async Pathfinding - defer instead of using callback so memory is set on main thread (DivineMC)
             Path path = AcquirePoi.findPathToPois(body, pois);
-            if (path != null && path.canReach()) {
-                BlockPos targetPos = path.getTarget();
-                Optional<Holder<PoiType>> type = poiManager.getType(targetPos);
-                if (type.isPresent()) {
-                    body.getBrain().setMemory(MemoryModuleType.NEAREST_BED, targetPos);
-                }
-            } else if (this.triedCount < 5) {
-                this.batchCache.long2LongEntrySet().removeIf(entry -> entry.getLongValue() < this.lastUpdate);
+            if (path instanceof org.bxteam.divinemc.async.pathfinding.AsyncPath asyncPath && !asyncPath.isProcessed()) {
+                this.pending = path;
+                return;
+            }
+            applyPathResult(body, poiManager, path);
+            // Purpur end - Async Pathfinding (DivineMC)
+        }
+    }
+
+    // Purpur start - Async Pathfinding (DivineMC)
+    private void applyPathResult(final Mob body, final PoiManager poiManager, final @org.jetbrains.annotations.Nullable Path path) {
+        if (path != null && path.canReach()) {
+            BlockPos targetPos = path.getTarget();
+            Optional<Holder<PoiType>> type = poiManager.getType(targetPos);
+            if (type.isPresent()) {
+                body.getBrain().setMemory(MemoryModuleType.NEAREST_BED, targetPos);
             }
+        } else if (this.triedCount < 5) {
+            this.batchCache.long2LongEntrySet().removeIf(entry -> entry.getLongValue() < this.lastUpdate);
         }
     }
+    // Purpur end - Async Pathfinding (DivineMC)
 }
diff --git a/net/minecraft/world/entity/animal/bee/Bee.java b/net/minecraft/world/entity/animal/bee/Bee.java
index d344b4bb8515fb66d1ed33a0e6870c96927e6c72..c1e3f0dd33e70410914be38097f29c101a1685f5 100644
--- a/net/minecraft/world/entity/animal/bee/Bee.java
+++ b/net/minecraft/world/entity/animal/bee/Bee.java
@@ -943,7 +943,7 @@ public class Bee extends Animal implements FlyingAnimal, NeutralMob {
                         } else {
                             Bee.this.pathfindRandomlyTowards(Bee.this.hivePos);
                         }
-                    } else {
+                    } else if (Bee.this.navigation.getPath() != null && Bee.this.navigation.getPath().isProcessed()) { // Purpur - check processing (DivineMC)
                         boolean canReachAllTheWayToTarget = this.pathfindDirectlyTowards(Bee.this.hivePos);
                         if (!canReachAllTheWayToTarget) {
                             this.dropAndBlacklistHive();
@@ -998,7 +998,7 @@ public class Bee extends Animal implements FlyingAnimal, NeutralMob {
             }
 
             Path path = Bee.this.navigation.getPath();
-            return path != null && path.getTarget().equals(targetPos) && path.canReach() && path.isDone();
+            return path != null && path.isProcessed() && path.getTarget().equals(targetPos) && path.canReach() && path.isDone(); // Purpur - ensure path is processed (DivineMC)
         }
     }
 
diff --git a/net/minecraft/world/entity/animal/frog/Frog.java b/net/minecraft/world/entity/animal/frog/Frog.java
index 94cd1064b8544060f2838fb776d95c19bcaf629b..3424456ef834ec87c1bf06e6e6493f41f9f8b63c 100644
--- a/net/minecraft/world/entity/animal/frog/Frog.java
+++ b/net/minecraft/world/entity/animal/frog/Frog.java
@@ -445,6 +445,17 @@ public class Frog extends Animal {
             super(mob, level);
         }
 
+        // Purpur start - Async Pathfinding (DivineMC)
+        private static final org.bxteam.divinemc.async.pathfinding.NodeEvaluatorGenerator nodeEvaluatorGenerator = (org.bxteam.divinemc.async.pathfinding.NodeEvaluatorFeatures nodeEvaluatorFeatures) -> {
+            Frog.FrogNodeEvaluator nodeEvaluator = new Frog.FrogNodeEvaluator(true);
+            nodeEvaluator.setCanPassDoors(nodeEvaluatorFeatures.canPassDoors());
+            nodeEvaluator.setCanFloat(nodeEvaluatorFeatures.canFloat());
+            nodeEvaluator.setCanWalkOverFences(nodeEvaluatorFeatures.canWalkOverFences());
+            nodeEvaluator.setCanOpenDoors(nodeEvaluatorFeatures.canOpenDoors());
+            return nodeEvaluator;
+        };
+        // Purpur end - Async Pathfinding (DivineMC)
+
         @Override
         public boolean canCutCorner(final PathType pathType) {
             return pathType != PathType.WATER_BORDER && super.canCutCorner(pathType);
@@ -453,6 +464,11 @@ public class Frog extends Animal {
         @Override
         protected PathFinder createPathFinder(final int maxVisitedNodes) {
             this.nodeEvaluator = new Frog.FrogNodeEvaluator(true);
+            // Purpur start - Async Pathfinding (DivineMC)
+            if (org.bxteam.divinemc.config.DivineConfig.AsyncCategory.asyncPathfinding) {
+                return new PathFinder(this.nodeEvaluator, maxVisitedNodes, nodeEvaluatorGenerator);
+            }
+            // Purpur end - Async Pathfinding (DivineMC)
             return new PathFinder(this.nodeEvaluator, maxVisitedNodes);
         }
     }
diff --git a/net/minecraft/world/entity/animal/rabbit/Rabbit.java b/net/minecraft/world/entity/animal/rabbit/Rabbit.java
index a6ccaafe6c025bda6b82be87ff4689117cc7fbf3..16a9f4b1eb9c2137a3a30ae450e19bcf093d843d 100644
--- a/net/minecraft/world/entity/animal/rabbit/Rabbit.java
+++ b/net/minecraft/world/entity/animal/rabbit/Rabbit.java
@@ -660,6 +660,14 @@ public class Rabbit extends Animal {
 
         @Override
         public void vanillaTick() { // Purpur - Ridables
+            // Purpur start - Async Pathfinding (DivineMC)
+            if (org.bxteam.divinemc.config.DivineConfig.AsyncCategory.asyncPathfinding) {
+                Path path = this.rabbit.getNavigation().getPath();
+                if (path != null && !path.isProcessed()) {
+                    return;
+                }
+            }
+            // Purpur end - Async Pathfinding (DivineMC)
             if (this.rabbit.onGround() && !this.rabbit.jumping && !((Rabbit.RabbitJumpControl)this.rabbit.jumpControl).wantJump()) {
                 this.rabbit.setSpeedModifier(0.0);
             } else if (this.hasWanted() || this.operation == MoveControl.Operation.JUMPING) {
diff --git a/net/minecraft/world/entity/monster/Strider.java b/net/minecraft/world/entity/monster/Strider.java
index 554c3017510331dc18215b1ea0da4d4f36c73fa9..c9ebef9fbb44a739df915ec6a88766086efbff94 100644
--- a/net/minecraft/world/entity/monster/Strider.java
+++ b/net/minecraft/world/entity/monster/Strider.java
@@ -566,9 +566,25 @@ public class Strider extends Animal implements ItemSteerable {
             super(mob, level);
         }
 
+        // Purpur start - Async Pathfinding (DivineMC)
+        private static final org.bxteam.divinemc.async.pathfinding.NodeEvaluatorGenerator nodeEvaluatorGenerator = (org.bxteam.divinemc.async.pathfinding.NodeEvaluatorFeatures nodeEvaluatorFeatures) -> {
+            WalkNodeEvaluator nodeEvaluator = new WalkNodeEvaluator();
+            nodeEvaluator.setCanPassDoors(nodeEvaluatorFeatures.canPassDoors());
+            nodeEvaluator.setCanFloat(nodeEvaluatorFeatures.canFloat());
+            nodeEvaluator.setCanWalkOverFences(nodeEvaluatorFeatures.canWalkOverFences());
+            nodeEvaluator.setCanOpenDoors(nodeEvaluatorFeatures.canOpenDoors());
+            return nodeEvaluator;
+        };
+        // Purpur end - Async Pathfinding (DivineMC)
+
         @Override
         protected PathFinder createPathFinder(final int maxVisitedNodes) {
             this.nodeEvaluator = new WalkNodeEvaluator();
+            // Purpur start - Async Pathfinding (DivineMC)
+            if (org.bxteam.divinemc.config.DivineConfig.AsyncCategory.asyncPathfinding) {
+                return new PathFinder(this.nodeEvaluator, maxVisitedNodes, nodeEvaluatorGenerator);
+            }
+            // Purpur end (DivineMC)
             return new PathFinder(this.nodeEvaluator, maxVisitedNodes);
         }
 
diff --git a/net/minecraft/world/entity/monster/warden/Warden.java b/net/minecraft/world/entity/monster/warden/Warden.java
index 4af25b2afa2cb8a2fec84d9281bd386d1517a644..df2367347b75e61d421bde206ce5982ae1c882a3 100644
--- a/net/minecraft/world/entity/monster/warden/Warden.java
+++ b/net/minecraft/world/entity/monster/warden/Warden.java
@@ -579,6 +579,16 @@ public class Warden extends Monster implements VibrationSystem {
             @Override
             protected PathFinder createPathFinder(final int maxVisitedNodes) {
                 this.nodeEvaluator = new WalkNodeEvaluator();
+                // Purpur start - Async Pathfinding (DivineMC)
+                if (org.bxteam.divinemc.config.DivineConfig.AsyncCategory.asyncPathfinding) {
+                    return new PathFinder(this.nodeEvaluator, maxVisitedNodes, GroundPathNavigation.nodeEvaluatorGenerator) {
+                        @Override
+                        protected float distance(Node first, Node second) {
+                            return first.distanceToXZ(second);
+                        }
+                    };
+                }
+                // Purpur end - Async Pathfinding (DivineMC)
                 return new PathFinder(this.nodeEvaluator, maxVisitedNodes) {
                     @Override
                     protected float distance(final Node from, final Node to) {
diff --git a/net/minecraft/world/entity/monster/zombie/Drowned.java b/net/minecraft/world/entity/monster/zombie/Drowned.java
index 4801c6987740f846a3049bfa0fcac547183e3f13..afdc8ea8b3d8dfcecac269ff2c655b1c6d63e32b 100644
--- a/net/minecraft/world/entity/monster/zombie/Drowned.java
+++ b/net/minecraft/world/entity/monster/zombie/Drowned.java
@@ -343,7 +343,7 @@ public class Drowned extends Zombie implements RangedAttackMob {
 
     protected boolean closeToNextPos() {
         Path path = this.getNavigation().getPath();
-        if (path != null) {
+        if (path != null && path.isProcessed()) { // Purpur - Async Pathfinding (DivineMC)
             BlockPos pos = path.getTarget();
             if (pos != null) {
                 double sqrDistToNextPos = this.distanceToSqr(pos.getX(), pos.getY(), pos.getZ());
diff --git a/net/minecraft/world/level/pathfinder/NodeEvaluator.java b/net/minecraft/world/level/pathfinder/NodeEvaluator.java
index f4a30761483c4032b5942f84df7706b10976cb5d..ae0c9e595aaca692c4691337e18f508b48af2f7b 100644
--- a/net/minecraft/world/level/pathfinder/NodeEvaluator.java
+++ b/net/minecraft/world/level/pathfinder/NodeEvaluator.java
@@ -37,6 +37,18 @@ public abstract class NodeEvaluator {
         this.mob = null;
     }
 
+    // Purpur start - Async Pathfinding (DivineMC)
+    public void clearCache() {
+        if (this.currentContext != null) {
+            this.currentContext.cache = null;
+        }
+    }
+
+    public void clearNodes() {
+        this.nodes.clear();
+    }
+    // Purpur end - Async Pathfinding (DivineMC)
+
     protected Node getNode(final BlockPos pos) {
         return this.getNode(pos.getX(), pos.getY(), pos.getZ());
     }
diff --git a/net/minecraft/world/level/pathfinder/Path.java b/net/minecraft/world/level/pathfinder/Path.java
index ce536d07512c3cb9a8f56a1c4f6506db50161f8b..0922b77c62d3906f3c9616392df8685e763efacc 100644
--- a/net/minecraft/world/level/pathfinder/Path.java
+++ b/net/minecraft/world/level/pathfinder/Path.java
@@ -11,9 +11,9 @@ import net.minecraft.world.entity.Entity;
 import net.minecraft.world.phys.Vec3;
 import org.jspecify.annotations.Nullable;
 
-public final class Path {
+public class Path { // Purpur - Async Pathfinding (DivineMC) - de-finalize to allow AsyncPath subclass
     public static final StreamCodec<FriendlyByteBuf, Path> STREAM_CODEC = StreamCodec.of((output, value) -> value.writeToStream(output), Path::createFromStream);
-    public final List<Node> nodes;
+    public List<Node> nodes; // Purpur - Async Pathfinding (DivineMC) - de-finalize to allow async result swap
     private Path.@Nullable DebugData debugData;
     private int nextNodeIndex;
     private final BlockPos target;
@@ -27,6 +27,17 @@ public final class Path {
         this.reached = reached;
     }
 
+    // Purpur start - Async Pathfinding (DivineMC)
+    /**
+     * checks if the path is completely processed in the case of it being computed async
+     *
+     * @return true if the path is processed
+     */
+    public boolean isProcessed() {
+        return true;
+    }
+    // Purpur end - Async Pathfinding (DivineMC)
+
     public void advance() {
         this.nextNodeIndex++;
     }
@@ -98,6 +109,7 @@ public final class Path {
     }
 
     public boolean sameAs(final @Nullable Path path) {
+        if (path == this) return true; // Purpur - Async Pathfinding (DivineMC)
         return path != null && this.nodes.equals(path.nodes);
     }
 
diff --git a/net/minecraft/world/level/pathfinder/PathFinder.java b/net/minecraft/world/level/pathfinder/PathFinder.java
index 253074a0a14115cd3d18aaae6fa5822cf32346c9..4b966def187a16e422ff9dbfa0bbbb3fbf0f877c 100644
--- a/net/minecraft/world/level/pathfinder/PathFinder.java
+++ b/net/minecraft/world/level/pathfinder/PathFinder.java
@@ -19,6 +19,8 @@ import net.minecraft.world.entity.Mob;
 import net.minecraft.world.level.PathNavigationRegion;
 import org.jspecify.annotations.Nullable;
 
+import org.bxteam.divinemc.async.pathfinding.NodeEvaluatorGenerator; // Purpur - Petal: Async Pathfinding (DivineMC)
+
 public class PathFinder {
     private static final float FUDGING = 1.5F;
     private final Node[] neighbors = new Node[32];
@@ -26,11 +28,19 @@ public class PathFinder {
     public final NodeEvaluator nodeEvaluator;
     private final BinaryHeap openSet = new BinaryHeap();
     private BooleanSupplier captureDebug = () -> false;
+    private final @Nullable NodeEvaluatorGenerator nodeEvaluatorGenerator; // Purpur - Async Pathfinding (DivineMC)
 
-    public PathFinder(final NodeEvaluator nodeEvaluator, final int maxVisitedNodes) {
+    // Purpur start - Async Pathfinding (DivineMC)
+    public PathFinder(final NodeEvaluator nodeEvaluator, final int maxVisitedNodes, final @Nullable NodeEvaluatorGenerator nodeEvaluatorGenerator) {
         this.nodeEvaluator = nodeEvaluator;
         this.maxVisitedNodes = maxVisitedNodes;
+        this.nodeEvaluatorGenerator = nodeEvaluatorGenerator;
+    }
+
+    public PathFinder(final NodeEvaluator nodeEvaluator, final int maxVisitedNodes) {
+        this(nodeEvaluator, maxVisitedNodes, null);
     }
+    // Purpur end - Async Pathfinding (DivineMC)
 
     public void setCaptureDebug(final BooleanSupplier captureDebug) {
         this.captureDebug = captureDebug;
@@ -48,27 +58,96 @@ public class PathFinder {
         final int reachRange,
         final float maxVisitedNodesMultiplier
     ) {
-        this.openSet.clear();
-        this.nodeEvaluator.prepare(level, entity);
-        Node from = this.nodeEvaluator.getStart();
+        // Purpur start - Async Pathfinding (DivineMC)
+        NodeEvaluator nodeEvaluator = this.nodeEvaluatorGenerator == null
+            ? this.nodeEvaluator
+            : org.bxteam.divinemc.async.pathfinding.NodeEvaluatorCache.takeNodeEvaluator(this.nodeEvaluatorGenerator, this.nodeEvaluator);
+
+        nodeEvaluator.prepare(level, entity);
+        nodeEvaluator.clearCache();
+        Node from = nodeEvaluator.getStart();
         if (from == null) {
+            if (nodeEvaluator != this.nodeEvaluator) {
+                nodeEvaluator.done();
+                org.bxteam.divinemc.async.pathfinding.NodeEvaluatorCache.removeNodeEvaluator(nodeEvaluator);
+            }
             return null;
         }
 
         // Paper start - Perf: remove streams and optimize collection
         List<Map.Entry<Target, BlockPos>> tos = Lists.newArrayList();
         for (BlockPos pos : targets) {
-            tos.add(new java.util.AbstractMap.SimpleEntry<>(this.nodeEvaluator.getTarget(pos.getX(), pos.getY(), pos.getZ()), pos));
+            tos.add(new java.util.AbstractMap.SimpleEntry<>(nodeEvaluator.getTarget(pos.getX(), pos.getY(), pos.getZ()), pos));
         }
         // Paper end - Perf: remove streams and optimize collection
-        Path path = this.findPath(from, tos, maxPathLength, reachRange, maxVisitedNodesMultiplier);
-        this.nodeEvaluator.done();
-        return path;
+
+        if (this.nodeEvaluatorGenerator == null) {
+            Path path = this.findPath(from, tos, maxPathLength, reachRange, maxVisitedNodesMultiplier);
+            this.nodeEvaluator.done();
+            return path;
+        }
+
+        final int maxVisitedNodes = this.maxVisitedNodes;
+        final BooleanSupplier captureDebug = this.captureDebug;
+        final PathNavigationRegion regionCapture = level;
+        final Mob mobCapture = entity;
+        final NodeEvaluatorGenerator generatorCapture = this.nodeEvaluatorGenerator;
+
+        final int nodeEvaluatorFeatures = org.bxteam.divinemc.async.pathfinding.NodeEvaluatorFeatures.fromNodeEvaluator(nodeEvaluator);
+
+        nodeEvaluator.done();
+        org.bxteam.divinemc.async.pathfinding.NodeEvaluatorCache.returnNodeEvaluator(nodeEvaluator);
+
+        return new org.bxteam.divinemc.async.pathfinding.AsyncPath(this, Lists.newArrayList(), targets, finder -> {
+            NodeEvaluator asyncEvaluator = generatorCapture.generate(
+                org.bxteam.divinemc.async.pathfinding.NodeEvaluatorFeatures.unpack(nodeEvaluatorFeatures)
+            );
+
+            try {
+                asyncEvaluator.prepare(regionCapture, mobCapture);
+                asyncEvaluator.clearCache();
+
+                Node asyncStart = asyncEvaluator.getStart();
+                if (asyncStart == null) {
+                    return new Path(Lists.newArrayList(), targets.iterator().next(), false);
+                }
+
+                List<Map.Entry<Target, BlockPos>> asyncTos = Lists.newArrayList();
+                for (BlockPos pos : targets) {
+                    asyncTos.add(new java.util.AbstractMap.SimpleEntry<>(
+                        asyncEvaluator.getTarget(pos.getX(), pos.getY(), pos.getZ()), pos
+                    ));
+                }
+
+                return finder.findPath(
+                    asyncEvaluator,
+                    org.bxteam.divinemc.async.pathfinding.NodeEvaluatorCache.HEAP_LOCAL.get(),
+                    org.bxteam.divinemc.async.pathfinding.NodeEvaluatorCache.NEIGHBORS_LOCAL.get(),
+                    maxVisitedNodes,
+                    captureDebug,
+                    asyncStart,
+                    asyncTos,
+                    maxPathLength,
+                    reachRange,
+                    maxVisitedNodesMultiplier
+                );
+            } finally {
+                asyncEvaluator.done();
+                asyncEvaluator.clearNodes();
+            }
+        });
+        // Purpur end - Async Pathfinding (DivineMC)
     }
 
+    // Purpur start - Async Pathfinding (DivineMC)
     private @Nullable Path findPath(
         final Node from, final List<Map.Entry<Target, BlockPos>> targets, final float maxPathLength, final int reachRange, final float maxVisitedNodesMultiplier // Paper - optimize collection
     ) {
+        return findPath(this.nodeEvaluator, this.openSet, this.neighbors, this.maxVisitedNodes, this.captureDebug, from, targets, maxPathLength, reachRange, maxVisitedNodesMultiplier);
+    }
+
+    private Path findPath(final NodeEvaluator nodeEvaluator, final BinaryHeap openSet, final Node[] neighborsArr, final int maxVisitedNodes, final BooleanSupplier captureDebugSupplier, final Node from, final List<Map.Entry<Target, BlockPos>> targets, final float maxPathLength, final int reachRange, final float maxVisitedNodesMultiplier) {
+    // Purpur end - Async Pathfinding (DivineMC)
         ProfilerFiller profiler = Profiler.get();
         profiler.push("find_path");
         profiler.markForCharting(MetricCategory.PATH_FINDING);
@@ -76,20 +155,20 @@ public class PathFinder {
         from.g = 0.0F;
         from.h = this.getBestH(from, targets);
         from.f = from.h;
-        this.openSet.clear();
-        this.openSet.insert(from);
-        boolean captureDebug = this.captureDebug.getAsBoolean();
+        openSet.clear(); // Purpur - Async Pathfinding (DivineMC)
+        openSet.insert(from); // Purpur - Async Pathfinding (DivineMC)
+        boolean captureDebug = captureDebugSupplier.getAsBoolean(); // Purpur - Async Pathfinding (DivineMC)
         Set<Node> closedSet = captureDebug ? new HashSet<>() : Set.of();
         int count = 0;
         List<Map.Entry<Target, BlockPos>> reachedTargets = Lists.newArrayListWithExpectedSize(targets.size()); // Paper - optimize collection
-        int maxVisitedNodesAdjusted = (int)(this.maxVisitedNodes * maxVisitedNodesMultiplier);
+        int maxVisitedNodesAdjusted = (int)(maxVisitedNodes * maxVisitedNodesMultiplier); // Purpur - Async Pathfinding (DivineMC)
 
-        while (!this.openSet.isEmpty()) {
+        while (!openSet.isEmpty()) { // Purpur - Async Pathfinding (DivineMC)
             if (++count >= maxVisitedNodesAdjusted) {
                 break;
             }
 
-            Node current = this.openSet.pop();
+            Node current = openSet.pop(); // Purpur - Async Pathfinding (DivineMC)
             current.closed = true;
 
             // Paper start - Perf: remove streams and optimize collection
@@ -112,10 +191,10 @@ public class PathFinder {
             }
 
             if (!(current.distanceTo(from) >= maxPathLength)) {
-                int neighborCount = this.nodeEvaluator.getNeighbors(this.neighbors, current);
+                int neighborCount = nodeEvaluator.getNeighbors(neighborsArr, current); // Purpur - Async Pathfinding (DivineMC)
 
                 for (int i = 0; i < neighborCount; i++) {
-                    Node neighbor = this.neighbors[i];
+                    Node neighbor = neighborsArr[i]; // Purpur - Async Pathfinding (DivineMC)
                     float distance = this.distance(current, neighbor);
                     neighbor.walkedDistance = current.walkedDistance + distance;
                     float tentativeGScore = current.g + distance + neighbor.costMalus;
@@ -124,10 +203,10 @@ public class PathFinder {
                         neighbor.g = tentativeGScore;
                         neighbor.h = this.getBestH(neighbor, targets) * 1.5F;
                         if (neighbor.inOpenSet()) {
-                            this.openSet.changeCost(neighbor, neighbor.g + neighbor.h);
+                            openSet.changeCost(neighbor, neighbor.g + neighbor.h); // Purpur - Async Pathfinding (DivineMC)
                         } else {
                             neighbor.f = neighbor.g + neighbor.h;
-                            this.openSet.insert(neighbor);
+                            openSet.insert(neighbor); // Purpur - Async Pathfinding (DivineMC)
                         }
                     }
                 }
@@ -152,9 +231,9 @@ public class PathFinder {
             for(Map.Entry<Target, BlockPos> entry : targets) {
                 set.add(entry.getKey());
             }
-            best.setDebug(this.openSet.getHeap(), closedSet.toArray(Node[]::new), set);
+            best.setDebug(openSet.getHeap(), closedSet.toArray(Node[]::new), set); // Purpur - Async Pathfinding (DivineMC)
         }
-        return best;
+        return java.util.Objects.requireNonNull(best); // Purpur - Async Pathfinding (DivineMC)
         // Paper end - Perf: remove streams and optimize collection
     }
 
diff --git a/net/minecraft/world/level/pathfinder/PathfindingContext.java b/net/minecraft/world/level/pathfinder/PathfindingContext.java
index 64e1a34de133beccb90486f247e7e74d6535941f..ef477cfbebe7a10ceafacf148b58cf4d04eeb6a8 100644
--- a/net/minecraft/world/level/pathfinder/PathfindingContext.java
+++ b/net/minecraft/world/level/pathfinder/PathfindingContext.java
@@ -9,7 +9,7 @@ import org.jspecify.annotations.Nullable;
 
 public class PathfindingContext {
     private final CollisionGetter level;
-    private final @Nullable PathTypeCache cache;
+    @Nullable PathTypeCache cache; // Purpur - Async Pathfinding (DivineMC) - de-finalize for NodeEvaluator#clearCache
     private final BlockPos mobPosition;
     private final BlockPos.MutableBlockPos mutablePos = new BlockPos.MutableBlockPos();
 
diff --git a/net/minecraft/world/level/pathfinder/SwimNodeEvaluator.java b/net/minecraft/world/level/pathfinder/SwimNodeEvaluator.java
index e55558448f91410f6174548edc88327191358825..7105a6d7afd23736d091a203dd0c29119a775b83 100644
--- a/net/minecraft/world/level/pathfinder/SwimNodeEvaluator.java
+++ b/net/minecraft/world/level/pathfinder/SwimNodeEvaluator.java
@@ -15,7 +15,7 @@ import net.minecraft.world.level.material.FluidState;
 import org.jspecify.annotations.Nullable;
 
 public class SwimNodeEvaluator extends NodeEvaluator {
-    private final boolean allowBreaching;
+    public final boolean allowBreaching; // Purpur - Async Pathfinding (DivineMC) - expose for NodeEvaluatorFeatures
     private final Long2ObjectMap<PathType> pathTypesByPosCache = new Long2ObjectOpenHashMap<>();
 
     public SwimNodeEvaluator(final boolean allowBreaching) {
